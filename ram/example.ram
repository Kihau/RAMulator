; The following code does not actually do anything

; this label gets ignored, main is not necessary
main:
    READ 1
    JUMP other
    ; this is a comment
    LOAD 2
    STORE =3
looping: 
other: LOAD 1
    JUMP something ; this is an inline comment
    ADD 2
something: 
    WRITE 2
    JUMP looping
    HALT
